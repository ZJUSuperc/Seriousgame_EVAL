[pytest]
# importlib mode so that the `tests/modeling/` package does not shadow the
# top-level `modeling/` package during test collection. This keeps the brief's
# bare command `python -m pytest tests/modeling/test_labels.py -v` working
# without per-invocation flags.
addopts = --import-mode=importlib
